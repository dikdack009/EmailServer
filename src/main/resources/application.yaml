spring:
  thymeleaf:
    cache: false
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${SENDER_USERNAME}
    password: ${SENDER_PASSWORD}
    properties:
      mail:
        smtp:
          starttls:
            required: true
            enable: true
